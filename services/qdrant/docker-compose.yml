services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: kaare-qdrant
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - /kaare/state/qdrant:/qdrant/storage
    env_file:
      - path: /kaare/configs/qdrant.env
        required: false
